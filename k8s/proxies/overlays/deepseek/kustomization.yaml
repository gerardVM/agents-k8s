apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: deepseek-proxy
resources:
  - namespace.yaml
  - ../../base

namePrefix: deepseek-
labels:
  - pairs:
      app: deepseek-api-proxy
    includeSelectors: true

generatorOptions: {}

configMapGenerator:
  - name: proxy-code
    files:
      - index.mjs
      - package.json
